env:
- TEST_SUITE=test
- TEST_SUITE=dialyzer-travis

script: make all && make $TEST_SUITE

language: erlang
otp_release:
    - R16B02
services:
    - cassandra

cache:
  apt: true
  directories:
    - deps
